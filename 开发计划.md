# 文章阅读 App 开发计划

**当前版本**: v1.1  
**最后更新**: 2025-11-27
**开发状态**: 🟢 进行中

> **文档说明**: 本文档记录项目的完整开发计划。所有需求变更都会在文档末尾的"变更记录"章节中记录。

---

## 📋 需求分析

### 核心功能
1. **文章内容获取**: 通过 API 请求获取文章内容
2. **身份认证**: 使用 JWT Token 进行 API 身份认证
3. **Token 管理**: 
   - 初始 Token 从环境变量获取
   - 客户端管理 Token（存储、更新、刷新）
   - 使用加密 Cookie 保存和传输
4. **环境区分**: 
   - 开发环境：无需 JWT 认证
   - 生产环境：需要 JWT 认证
5. **文章展示**: 显示文章标题、内容、作者、上下章链接、书名、描述等信息
6. **书籍管理**: 
   - 通过输入书籍 URL 添加书籍（URL 格式：`https://quanben.io/n/{book-id}/{chapter-number}.html`）
   - 书籍列表管理（查看、删除）
   - 每个 URL 对应一本书
7. **阅读进度管理**: 
   - 保存每本书的当前阅读章节
   - 支持继续阅读功能
   - 阅读进度持久化存储
8. **本地存储**: 
   - 使用 localStorage 存储书籍列表
   - 使用 localStorage 存储阅读进度

### API 接口规范

**开发环境**:
- URL: `http://localhost:3000/api/extract`
- 认证: 无需 JWT
- Method: POST
- Body: `{ "url": "https://quanben.io/n/xuanjiezhimen/1.html" }`

**生产环境**:
- URL: `https://capy-book-fetch.vercel.app/api/extract`
- 认证: 需要 JWT Token（通过 `Authorization: Bearer <token>` 头传输）
- Method: POST
- Body: `{ "url": "https://quanben.io/n/xuanjiezhimen/1.html" }`
- **注意**: Token 在客户端 Cookie 中加密存储，发送给 API 前解密为原始 JWT

**响应格式**:
```typescript
{
  "success": true,
  "data": {
    "title": "序章",
    "content": "文章内容...",
    "author": "",
    "prevLink": null,
    "nextLink": "https://quanben.io/n/xuanjiezhimen/2.html",
    "bookName": "玄界之门",
    "description": "《玄界之门》序章，简介:..."
  }
}
```

**URL 格式规范**:
- 格式：`https://quanben.io/n/{book-id}/{chapter-number}.html`
- 示例：`https://quanben.io/n/xuanjiezhimen/1.html`
- book-id: 书籍唯一标识（如：`xuanjiezhimen`）
- chapter-number: 章节编号（如：`1`）

## 📦 依赖包规划

### UI 组件库

**使用 [shadcn/ui](https://ui.shadcn.com/)**:
- 基于 Tailwind CSS 和 Radix UI 的组件库
- 通过 CLI 将组件代码复制到项目中，可完全自定义
- 与 Next.js App Router 完美兼容
- 组件放在 `components/ui/` 目录

### 需要安装的依赖

```json
{
  "dependencies": {
    "next": "16.0.5",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "jose": "^5.2.0",                         // JWT 处理（服务端）和 Cookie 加密
    // shadcn/ui 核心依赖
    "class-variance-authority": "^0.7.0",     // 组件变体管理
    "clsx": "^2.1.0",                         // 类名工具
    "tailwind-merge": "^2.2.0",               // Tailwind 类名合并
    "lucide-react": "^0.344.0",              // 图标库
    "@radix-ui/react-slot": "^1.0.2"          // Radix UI Slot 组件
  },
  "devDependencies": {
    // shadcn/ui CLI（全局安装或使用 npx）
    // "shadcn-ui": "latest"                  // 使用 npx shadcn@latest 即可
    // ... 现有依赖
  }
}
```

**说明**:
- **Cookie 操作**: 使用 Next.js 原生方案（封装 `document.cookie`），无需额外依赖
- `jose`: 服务端 JWT 验证和加密（用于 Cookie 加密）
- shadcn/ui 相关依赖：用于组件样式和功能
- shadcn/ui 安装：使用 `npx shadcn@latest init` 初始化，然后 `npx shadcn@latest add <component>` 添加组件

## 📁 项目目录结构

```
src/
├── app/
│   ├── layout.tsx                    # 根布局（更新元数据）
│   ├── page.tsx                      # 首页（文章阅读页面）
│   ├── loading.tsx                   # 加载状态
│   ├── error.tsx                     # 错误边界
│   ├── not-found.tsx                 # 404 页面
│   └── globals.css                   # 全局样式
├── components/
│   ├── ui/                           # shadcn/ui 基础组件（通过 CLI 添加）
│   │   ├── button.tsx                # 按钮组件（shadcn/ui，小写命名）
│   │   ├── input.tsx                 # 输入框组件（shadcn/ui，小写命名）
│   │   ├── card.tsx                  # 卡片组件（shadcn/ui，小写命名）
│   │   ├── skeleton.tsx              # 骨架屏组件（shadcn/ui，小写命名）
│   │   └── ...                       # 其他 shadcn/ui 组件
│   ├── article/                      # 文章相关组件
│   │   ├── ArticleReader.tsx         # 文章阅读器主组件（PascalCase）
│   │   ├── ArticleHeader.tsx         # 文章头部（PascalCase）
│   │   ├── ArticleContent.tsx        # 文章内容展示（PascalCase）
│   │   └── ArticleNavigation.tsx     # 文章导航（PascalCase）
│   ├── books/                        # 书籍管理组件
│   │   ├── UrlInput.tsx              # URL 输入组件（PascalCase）
│   │   ├── BookList.tsx              # 书籍列表组件（PascalCase）
│   │   └── BookCard.tsx              # 书籍卡片组件（PascalCase）
│   └── layout/                       # 布局组件
│       └── Header.tsx                # 页面头部（可选，PascalCase）
├── lib/
│   ├── api/                          # API 相关
│   │   ├── client.ts                 # API 客户端封装（camelCase）
│   │   └── types.ts                  # API 类型定义（camelCase）
│   ├── auth/                         # 认证相关
│   │   ├── token.ts                  # Token 管理（camelCase）
│   │   ├── cookie.ts                 # Cookie 操作（camelCase）
│   │   └── jwt.ts                     # JWT 工具函数（camelCase）
│   ├── books/                        # 书籍管理相关
│   │   ├── urlParser.ts              # URL 解析工具（camelCase）
│   │   ├── manager.ts                # 书籍管理逻辑（camelCase）
│   │   └── progress.ts               # 阅读进度管理（camelCase）
│   ├── storage/                      # 存储相关
│   │   └── localStorage.ts            # localStorage 封装（camelCase）
│   └── utils.ts                      # 通用工具函数（camelCase，shadcn/ui 的 cn 函数）
├── hooks/                            # 自定义 Hooks
│   ├── useArticle.ts                 # 文章数据获取 Hook（camelCase）
│   ├── useToken.ts                   # Token 管理 Hook（camelCase）
│   ├── useBooks.ts                   # 书籍管理 Hook（camelCase）
│   └── useReadingProgress.ts         # 阅读进度 Hook（camelCase）
├── types/                            # TypeScript 类型定义
│   ├── article.ts                    # 文章相关类型（camelCase）
│   ├── api.ts                        # API 响应类型（camelCase）
│   ├── book.ts                       # 书籍相关类型（camelCase）
│   └── index.ts                      # 类型导出（camelCase）
└── middleware.ts                     # Next.js 中间件（可选，用于 Token 刷新）
```

**命名规范说明**:
- **shadcn/ui 组件**: 使用小写命名（`button.tsx`），这是 shadcn/ui 的约定
- **自定义组件**: 使用 PascalCase（`ArticleReader.tsx`），符合 React 组件命名规范
- **工具文件**: 使用 camelCase（`token.ts`、`cookie.ts`），符合 JavaScript 命名规范
- **页面/布局文件**: 必须使用小写（`page.tsx`、`layout.tsx`），这是 Next.js 的约定

## 🎯 开发阶段规划

### 阶段 1: 项目基础搭建

#### 1.1 安装依赖
- [ ] 安装基础依赖
  - `jose`（用于 JWT 和 Cookie 加密）
  - **注意**: Cookie 操作使用 Next.js 原生方案，无需安装额外库
- [ ] 安装 shadcn/ui 依赖
  - `class-variance-authority`
  - `clsx`
  - `tailwind-merge`
  - `lucide-react`
  - `@radix-ui/react-slot`
- [ ] 初始化 shadcn/ui
  - 运行 `npx shadcn@latest init` 初始化配置
  - 配置 `components.json`（选择 TypeScript、Tailwind CSS、App Router）
  - 创建 `lib/utils.ts`（包含 `cn` 工具函数）
- [ ] 更新 `package.json`

#### 1.2 环境变量配置
创建 `.env.local` 文件：
```bash
# API 配置
NEXT_PUBLIC_API_URL_DEV=http://localhost:3000/api/extract
NEXT_PUBLIC_API_URL_PROD=https://capy-book-fetch.vercel.app/api/extract

# JWT Token（生产环境）
JWT_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJwcm9kdWN0aW9uLXVzZXIiLCJpYXQiOjE3NjQxNTEyOTMsImVudiI6InByb2R1Y3Rpb24iLCJnZW5lcmF0ZWRBdCI6IjIwMjUtMTEtMjZUMTA6MDE6MzMuNjc1WiIsImV4cCI6MTc2NDc1NjA5M30.KArp4o7FcSHhtotPSISkg39ecdLd6qzKHB0pUSsPPUc

# Cookie 加密密钥（用于加密存储 Token）
COOKIE_SECRET=your-secret-key-here

# 环境标识
NODE_ENV=development
```

#### 1.3 类型定义
- [ ] **`types/api.ts`**: API 请求/响应类型
  - 定义 API 请求体类型（`ExtractArticleRequest`）
  - 定义 API 响应类型（`ExtractArticleResponse`）
  - 定义错误响应类型
- [ ] **`types/article.ts`**: 文章数据类型
  - 定义文章数据接口（`ArticleData`）
  - 包含 title、content、author、prevLink、nextLink、bookName、description
- [ ] **`types/book.ts`**: 书籍相关类型
  - 定义书籍接口（`Book`）：bookId、bookName、currentChapter、lastReadUrl、addedAt
  - 定义阅读进度接口（`ReadingProgress`）：bookId、currentChapter、lastReadUrl、updatedAt
  - 定义书籍列表类型（`BookList`）
- [ ] **`types/index.ts`**: 类型导出
  - 统一导出所有类型，方便导入
- [ ] shadcn/ui 组件类型（自动生成，无需手动定义）
- [ ] **使用路径别名**: 所有类型导入使用 `@/types/*`

### 阶段 2: 核心功能开发

#### 2.1 Token 管理模块
- [ ] **`lib/auth/token.ts`**: Token 的获取、存储、更新逻辑
  - 从环境变量获取初始 Token（原始 JWT）
  - 从 Cookie 读取加密 Token 并解密得到原始 JWT
  - 将原始 JWT 加密后保存到 Cookie
  - Token 过期检查（解密后检查 JWT 的 exp 字段）

- [ ] **`lib/auth/cookie.ts`**: Cookie 操作和加密/解密
  - **使用 Next.js 原生方案**: 封装 `document.cookie` API（客户端）
  - 提供统一的 Cookie 操作接口：`setCookie()`, `getCookie()`, `removeCookie()`
  - 加密原始 JWT Token 后存储到 Cookie
  - 从 Cookie 读取加密值并解密得到原始 JWT Token
  - Cookie 配置（secure、sameSite、maxAge 等）
  - **关键**: 加密/解密操作，确保存储安全但不影响 API 认证
  - **实现方式**: 封装原生 `document.cookie`，支持完整的 Cookie 选项配置

- [ ] **`lib/auth/jwt.ts`**: JWT 工具函数
  - JWT 解析和验证（处理原始 JWT）
  - Token 过期时间检查（检查 exp 字段）
  - Token 格式验证

#### 2.2 API 客户端封装
- [ ] **`lib/api/client.ts`**: API 请求封装
  - **注意**: 如果需要在客户端使用，需要 `'use client'`；如果只在服务端使用，则不需要
  - 环境判断（开发/生产）
  - 自动添加 JWT Token（生产环境）
    - 从 Cookie 读取加密 Token（使用 `@/lib/auth/cookie`）
    - 解密得到原始 JWT（使用 `@/lib/auth/jwt`）
    - 添加到 `Authorization: Bearer <token>` 头
  - 请求拦截和错误处理
  - 响应数据格式化
  - **关键**: 确保发送给 API 的是原始 JWT，不是加密值
  - **使用路径别名**: `@/lib/auth/cookie`、`@/lib/auth/jwt`、`@/types/api`

#### 2.3 自定义 Hooks
- [ ] **`hooks/useArticle.ts`**: 文章数据获取 Hook
  - **必须添加 `'use client'`**（客户端 Hook）
  - 接收 URL 参数
  - 调用 API 获取文章（使用 `@/lib/api/client`）
  - 管理加载状态（useState）
  - 错误处理（try-catch）
  - **使用路径别名**: `@/lib/api/client`、`@/types/api`

- [ ] **`hooks/useToken.ts`**: Token 管理 Hook
  - **必须添加 `'use client'`**（客户端 Hook，需要访问 Cookie）
  - Token 状态管理（useState）
  - Token 更新方法
  - 自动从 Cookie 初始化（useEffect）
  - **使用路径别名**: `@/lib/auth/cookie`、`@/lib/auth/token`

### 阶段 2.5: 书籍管理功能开发

#### 2.4 localStorage 封装
- [ ] **`lib/storage/localStorage.ts`**: localStorage 操作封装
  - **必须添加 `'use client'`**（客户端操作）
  - 提供统一的存储接口：`getItem()`, `setItem()`, `removeItem()`, `clear()`
  - 错误处理（try-catch）
  - 类型安全（泛型支持）
  - **使用路径别名**: `@/types/book`

#### 2.5 URL 解析模块
- [ ] **`lib/books/urlParser.ts`**: URL 解析工具
  - 解析 URL 格式：`https://quanben.io/n/{book-id}/{chapter-number}.html`
  - 提取 bookId 和 chapterNumber
  - URL 格式验证
  - 错误处理（无效 URL）
  - **使用路径别名**: `@/types/book`

#### 2.6 书籍管理模块
- [ ] **`lib/books/manager.ts`**: 书籍管理逻辑
  - **必须添加 `'use client'`**（需要访问 localStorage）
  - 书籍列表管理（添加、删除、查询）
  - 与 localStorage 交互（使用 `@/lib/storage/localStorage`）
  - 书籍信息存储（bookId、bookName、currentChapter、lastReadUrl、addedAt）
  - 去重处理（相同 bookId 不重复添加）
  - **使用路径别名**: `@/lib/storage/localStorage`、`@/lib/books/urlParser`、`@/types/book`

#### 2.7 阅读进度管理模块
- [ ] **`lib/books/progress.ts`**: 阅读进度管理
  - **必须添加 `'use client'`**（需要访问 localStorage）
  - 保存当前阅读章节
  - 记录每本书的阅读进度（bookId、currentChapter、lastReadUrl、updatedAt）
  - 获取阅读进度
  - 更新阅读进度
  - 与 localStorage 交互
  - **使用路径别名**: `@/lib/storage/localStorage`、`@/types/book`

#### 2.8 书籍管理 Hooks
- [ ] **`hooks/useBooks.ts`**: 书籍管理 Hook
  - **必须添加 `'use client'`**（客户端 Hook）
  - 书籍列表状态管理（useState）
  - 添加书籍方法（接收 URL，解析后添加）
  - 删除书籍方法
  - 从 localStorage 初始化（useEffect）
  - **使用路径别名**: `@/lib/books/manager`、`@/lib/books/urlParser`、`@/types/book`

- [ ] **`hooks/useReadingProgress.ts`**: 阅读进度 Hook
  - **必须添加 `'use client'`**（客户端 Hook）
  - 阅读进度状态管理（useState）
  - 保存进度方法
  - 获取进度方法
  - 从 localStorage 初始化（useEffect）
  - **使用路径别名**: `@/lib/books/progress`、`@/types/book`

### 阶段 3: UI 组件开发

#### 3.1 安装 shadcn/ui 基础组件
- [ ] 添加额外的 shadcn/ui 组件（如需要）：
  ```bash
  npx shadcn@latest add alert        # 警告提示组件（用于错误提示）
  npx shadcn@latest add dialog       # 对话框组件（用于确认删除）
  ```
- [ ] 使用 CLI 添加需要的 shadcn/ui 组件：
  ```bash
  npx shadcn@latest add button      # 按钮组件
  npx shadcn@latest add input       # 输入框组件
  npx shadcn@latest add card        # 卡片组件
  npx shadcn@latest add skeleton    # 骨架屏组件（用于加载状态）
  ```
- [ ] 组件会自动添加到 `components/ui/` 目录
- [ ] 所有组件都可以根据需求自定义修改

#### 3.2 基础 UI 组件使用
- [ ] **按钮组件**: 使用 `components/ui/button.tsx`（shadcn/ui）
  - 支持多种变体（variant）和尺寸（size）
  - 用于文章导航按钮
- [ ] **输入框组件**: 使用 `components/ui/input.tsx`（shadcn/ui）
  - 用于输入文章 URL（如需要）
- [ ] **卡片组件**: 使用 `components/ui/card.tsx`（shadcn/ui）
  - 用于文章内容容器
- [ ] **骨架屏组件**: 使用 `components/ui/skeleton.tsx`（shadcn/ui）
  - 用于加载状态显示

#### 3.4 书籍管理组件
- [ ] **`components/books/UrlInput.tsx`**: URL 输入组件
  - **必须添加 `'use client'`**（需要交互）
  - 使用 shadcn/ui 的 Input 组件
  - URL 格式验证
  - 添加书籍功能（调用 `useBooks` Hook）
  - 错误提示（使用 shadcn/ui 的 Alert 组件）
  - **使用路径别名**: `@/components/ui/input`、`@/components/ui/button`、`@/hooks/useBooks`

- [ ] **`components/books/BookCard.tsx`**: 书籍卡片组件
  - **必须添加 `'use client'`**（需要交互）
  - 使用 shadcn/ui 的 Card 组件
  - 显示书籍信息（书名、当前章节、最后阅读时间）
  - 继续阅读按钮（跳转到最后阅读章节）
  - 删除按钮（带确认对话框）
  - **使用路径别名**: `@/components/ui/card`、`@/components/ui/button`、`@/hooks/useBooks`、`@/hooks/useReadingProgress`

- [ ] **`components/books/BookList.tsx`**: 书籍列表组件
  - **必须添加 `'use client'`**（需要交互）
  - 显示所有书籍列表
  - 使用 BookCard 组件渲染每个书籍
  - 空状态提示（无书籍时）
  - **使用路径别名**: `@/components/books/BookCard`、`@/hooks/useBooks`

#### 3.5 文章阅读组件
- [ ] **`components/article/ArticleReader.tsx`**: 主容器组件
  - **必须添加 `'use client'`**（需要客户端交互）
  - 整合所有子组件
  - 管理文章状态
  - 处理导航逻辑
  - **使用路径别名**: `@/components/ui/button`、`@/hooks/useArticle`

- [ ] **`components/article/ArticleHeader.tsx`**: 文章头部
  - 显示书名、标题、作者
  - 显示描述信息
  - 可以是服务端组件（如果不需要交互）

- [ ] **`components/article/ArticleContent.tsx`**: 文章内容
  - 使用 shadcn/ui 的 Card 组件作为容器
  - 格式化显示文章内容（换行处理）
  - 响应式布局（移动优先原则）
  - 阅读体验优化（字体、行距、间距）
  - 使用 Tailwind CSS 进行样式定制
  - 使用 `cn()` 函数合并类名

- [ ] **`components/article/ArticleNavigation.tsx`**: 导航组件
  - **必须添加 `'use client'`**（需要点击交互）
  - 使用 shadcn/ui 的 Button 组件
  - 上一章按钮（如果有 prevLink）
  - 下一章按钮（如果有 nextLink）
  - 点击跳转到对应章节
  - 使用 lucide-react 图标（如需要）
  - **使用路径别名**: `@/components/ui/button`

### 阶段 4: 页面开发

#### 4.1 首页（文章阅读页）
- [ ] **`app/page.tsx`**: 主页面
  - 书籍列表展示（使用 BookList 组件）
  - URL 输入功能（使用 UrlInput 组件）
  - 文章阅读器集成（选择书籍后显示）
  - 加载和错误状态处理
  - **使用路径别名**: `@/components/books/BookList`、`@/components/books/UrlInput`、`@/components/article/ArticleReader`

#### 4.2 路由设计（可选）
- [ ] 支持通过 URL 参数传递文章链接
  - 例如：`/?url=https://quanben.io/n/xuanjiezhimen/1.html`
  - 或使用动态路由：`/article?url=...`
- [ ] 支持从书籍列表跳转到阅读页面
  - 点击"继续阅读"按钮跳转到对应章节
  - 自动保存阅读进度

#### 4.3 错误处理
- [ ] **`app/error.tsx`**: 全局错误边界
- [ ] **`app/loading.tsx`**: 全局加载状态
- [ ] **`app/not-found.tsx`**: 404 页面

### 阶段 5: 样式和体验优化

#### 5.1 样式设计
- [ ] 使用 shadcn/ui 的样式系统
  - 基于 Tailwind CSS 的预设样式
  - 通过 `components.json` 配置主题
  - 使用 `cn()` 工具函数合并类名
- [ ] 使用 Tailwind CSS 设计文章阅读界面
  - 利用 shadcn/ui 组件的变体系统
  - 自定义组件样式（直接修改组件文件）
- [ ] 响应式布局（移动端适配）
  - shadcn/ui 组件默认支持响应式
- [ ] 暗色模式支持（可选）
  - shadcn/ui 内置暗色模式支持
  - 通过 Tailwind 的 `dark:` 前缀实现
- [ ] 阅读体验优化（字体大小、行距、背景色）
  - 使用 Tailwind CSS 工具类
  - 可自定义 shadcn/ui 组件的样式

#### 5.2 交互优化
- [ ] 加载状态提示
  - 使用 shadcn/ui 的 Skeleton 组件
  - 或使用 Button 的 loading 状态
- [ ] 错误提示和重试机制
  - 可添加 shadcn/ui 的 Alert 组件（如需要）
  - 使用 Button 组件实现重试按钮
- [ ] 章节切换动画（可选）
  - 使用 Tailwind CSS 的 transition 类
  - 或使用 Framer Motion（如需要）
- [ ] 键盘快捷键支持（可选，如左右箭头切换章节）
  - 使用 React 的键盘事件处理

### 阶段 6: 测试和优化

#### 6.1 功能测试
- [ ] 开发环境 API 请求测试
- [ ] 生产环境 JWT 认证测试
- [ ] Token 存储和读取测试
- [ ] Cookie 加密/解密测试
- [ ] 文章内容显示测试
- [ ] 章节导航测试
- [ ] URL 解析测试（正确格式、错误格式）
- [ ] 书籍管理测试（添加、删除、列表显示）
- [ ] 阅读进度测试（保存、读取、更新）
- [ ] localStorage 存储测试（持久化、错误处理）

#### 6.2 性能优化
- [ ] API 请求优化（缓存策略）
- [ ] 组件懒加载（如需要）
- [ ] 图片优化（如果有）
- [ ] 代码分割优化

#### 6.3 错误处理完善
- [ ] 网络错误处理
- [ ] API 错误响应处理
- [ ] Token 过期处理
- [ ] 无效 URL 处理

## 🔧 技术实现细节

### 1. Cookie 操作和加密方案

**Cookie 操作方案**:
- **使用 Next.js 原生方案**: 封装浏览器原生的 `document.cookie` API
- **优势**: 
  - 无需额外依赖（减少包体积）
  - 与 Next.js 完美集成
  - 完全控制 Cookie 行为
- **实现**: 在 `lib/auth/cookie.ts` 中封装统一的 Cookie 操作函数

**Cookie 加密方案**:
- 使用 `jose` 库进行加密/解密
- 或使用 `crypto` 模块（Node.js 内置）
- Cookie 配置：
  - `secure: true`（生产环境 HTTPS）
  - `sameSite: 'lax'`
  - `maxAge`: Token 过期时间
  - **注意**: 客户端 Cookie 无法设置 `httpOnly`（这是浏览器安全限制）

**Cookie 操作实现示例**:
```typescript
// lib/auth/cookie.ts
'use client';

interface CookieOptions {
  maxAge?: number;        // 秒数
  expires?: Date;         // 过期时间
  secure?: boolean;       // 仅 HTTPS
  sameSite?: 'strict' | 'lax' | 'none';
  path?: string;          // Cookie 路径
}

export function setCookie(name: string, value: string, options: CookieOptions = {}) {
  if (typeof window === 'undefined') {
    throw new Error('setCookie can only be used in client components');
  }

  let cookieString = `${encodeURIComponent(name)}=${encodeURIComponent(value)}`;

  if (options.maxAge) {
    cookieString += `; max-age=${options.maxAge}`;
  }
  
  if (options.expires) {
    cookieString += `; expires=${options.expires.toUTCString()}`;
  }
  
  if (options.secure) {
    cookieString += '; secure';
  }
  
  if (options.sameSite) {
    cookieString += `; samesite=${options.sameSite}`;
  }
  
  cookieString += `; path=${options.path || '/'}`;

  document.cookie = cookieString;
}

export function getCookie(name: string): string | undefined {
  if (typeof window === 'undefined') {
    return undefined;
  }

  const value = `; ${document.cookie}`;
  const parts = value.split(`; ${encodeURIComponent(name)}=`);
  
  if (parts.length === 2) {
    return decodeURIComponent(parts.pop()?.split(';').shift() || '');
  }
  
  return undefined;
}

export function removeCookie(name: string, options: CookieOptions = {}) {
  setCookie(name, '', { ...options, maxAge: 0 });
}
```

**重要说明 - 加密流程**:
- **加密目的**: 保护客户端存储的 Token，防止 XSS 攻击直接读取明文 Token
- **存储流程**: 原始 JWT Token → 加密 → 保存到 Cookie（使用 `setCookie()`）
- **读取流程**: 从 Cookie 读取（使用 `getCookie()`）→ 解密 → 得到原始 JWT Token
- **发送流程**: 原始 JWT Token → 添加到 `Authorization` 头 → 发送给 API 服务器
- **关键点**: API 服务器接收的是**原始 JWT Token**（解密后的），不是加密值。加密只用于客户端存储安全，不影响 API 认证。

### 2. 环境判断

```typescript
const isDevelopment = process.env.NODE_ENV === 'development';
const API_URL = isDevelopment 
  ? process.env.NEXT_PUBLIC_API_URL_DEV 
  : process.env.NEXT_PUBLIC_API_URL_PROD;
const NEED_AUTH = !isDevelopment;
```

### 3. Token 管理流程

1. **初始化**: 从环境变量 `JWT_TOKEN` 获取原始 JWT Token
2. **存储**: 原始 JWT Token → 加密 → 保存到 Cookie（加密存储）
3. **读取**: 从 Cookie 读取加密值 → 解密 → 得到原始 JWT Token
4. **使用**: 原始 JWT Token → 添加到 `Authorization: Bearer <token>` 头 → 发送给 API
5. **更新**: 如果 API 返回新 Token，加密后更新 Cookie
6. **验证**: 检查 Token 是否过期（解密后检查 JWT 的 exp 字段）

### 4. API 请求流程

```typescript
// 完整实现流程
async function fetchArticle(url: string) {
  // 1. 判断环境
  const apiUrl = getApiUrl();
  const needAuth = !isDevelopment();
  
  // 2. 准备请求头
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
  };
  
  // 3. 添加认证（生产环境）
  if (needAuth) {
    // 3.1 从 Cookie 读取加密的 Token
    const encryptedToken = getTokenFromCookie();
    
    // 3.2 解密得到原始 JWT Token
    const token = decryptToken(encryptedToken);
    
    // 3.3 使用 Authorization 头传输原始 JWT（方式1）
    headers['Authorization'] = `Bearer ${token}`;
    
    // 注意：API 服务器接收的是原始 JWT，可以正常验证签名
  }
  
  // 4. 发送请求
  const response = await fetch(apiUrl, {
    method: 'POST',
    headers,
    body: JSON.stringify({ url }),
  });
  
  // 5. 处理响应
  if (!response.ok) {
    throw new Error(`API 请求失败: ${response.statusText}`);
  }
  
  const data = await response.json();
  return data;
}
```

**认证传输方式说明**:
- **使用方式**: `Authorization: Bearer <原始JWT>` 头（方式1）
- **不使用**: Cookie 传输（方式2）
- **原因**: 更符合 RESTful 规范，API 服务器从 `Authorization` 头读取 Token

### 5. 文章内容格式化

- 处理 `\n` 换行符
- 段落间距
- 文本样式（字体、大小、颜色）
- 响应式布局

### 6. URL 解析实现

```typescript
// lib/books/urlParser.ts
export interface ParsedUrl {
  bookId: string;
  chapterNumber: number;
  fullUrl: string;
}

export function parseBookUrl(url: string): ParsedUrl | null {
  const pattern = /^https:\/\/quanben\.io\/n\/([^/]+)\/(\d+)\.html$/;
  const match = url.match(pattern);
  
  if (!match) {
    return null;
  }
  
  return {
    bookId: match[1],
    chapterNumber: parseInt(match[2], 10),
    fullUrl: url,
  };
}

export function isValidBookUrl(url: string): boolean {
  return parseBookUrl(url) !== null;
}
```

### 7. 书籍数据结构

```typescript
// types/book.ts
export interface Book {
  bookId: string;              // 书籍唯一标识
  bookName: string;            // 书籍名称
  currentChapter: number;      // 当前阅读章节
  lastReadUrl: string;         // 最后阅读的 URL
  addedAt: number;            // 添加时间戳
}

export interface ReadingProgress {
  bookId: string;
  currentChapter: number;
  lastReadUrl: string;
  updatedAt: number;           // 更新时间戳
}

export type BookList = Book[];
```

### 8. localStorage 存储键名

```typescript
// lib/storage/constants.ts
export const STORAGE_KEYS = {
  BOOKS: 'book-dis:books',                    // 书籍列表
  READING_PROGRESS: 'book-dis:reading-progress', // 阅读进度
} as const;
```

## 📝 开发注意事项

### 安全考虑
1. **Cookie 加密**: Token 必须加密存储（防止 XSS 读取明文）
   - 存储时：原始 JWT → 加密 → Cookie
   - 使用时：Cookie → 解密 → 原始 JWT → Authorization 头
2. **HTTPS**: 生产环境必须使用 HTTPS（保护传输安全）
3. **Token 过期**: 检查并处理 Token 过期情况（解密后检查 exp）
4. **XSS 防护**: 文章内容需要转义（React 自动处理）
5. **API 认证**: 发送原始 JWT 给 API，确保 API 可以正常验证签名

### 性能考虑
1. **API 缓存**: 相同 URL 的文章可以缓存
2. **懒加载**: 非关键组件使用动态导入
3. **代码分割**: 按路由分割代码

### 用户体验
1. **加载状态**: 显示清晰的加载提示（使用 shadcn/ui Skeleton）
2. **错误提示**: 友好的错误信息
3. **响应式设计**: 适配各种屏幕尺寸（shadcn/ui 组件默认支持）
4. **阅读体验**: 优化字体、行距、背景色

### shadcn/ui 使用注意事项
1. **组件自定义**: shadcn/ui 组件代码在项目中，可以直接修改
2. **样式定制**: 使用 Tailwind CSS 和 `cn()` 函数合并类名
3. **组件添加**: 使用 `npx shadcn@latest add <component>` 添加新组件
4. **类型安全**: 所有组件都有完整的 TypeScript 类型定义
5. **主题配置**: 通过 `components.json` 和 Tailwind 配置自定义主题
6. **图标使用**: 使用 `lucide-react` 图标库，按需导入

### Cookie 操作注意事项
1. **使用 Next.js 原生方案**: 封装 `document.cookie` API，无需额外依赖
2. **客户端限制**: Cookie 操作函数只能在客户端组件中使用（需要 `'use client'`）
3. **服务端处理**: 如需在服务端操作 Cookie，使用 `import { cookies } from 'next/headers'`
4. **编码处理**: 使用 `encodeURIComponent` 和 `decodeURIComponent` 处理特殊字符
5. **安全配置**: 生产环境必须设置 `secure: true` 和 `sameSite: 'lax'`
6. **httpOnly 限制**: 客户端 Cookie 无法设置 `httpOnly`（浏览器安全限制）

### 代码规范注意事项
1. **路径别名**: 必须使用 `@/*` 路径别名，禁止使用相对路径 `../`
2. **导入顺序**: 
   - React 和 Next.js 核心
   - 第三方库
   - 项目内部组件
   - 项目内部工具函数
   - 项目内部类型
   - 样式文件
3. **组件命名**: 
   - shadcn/ui 组件：小写（`button.tsx`）
   - 自定义组件：PascalCase（`ArticleReader.tsx`）
   - 工具文件：camelCase（`token.ts`）
4. **客户端组件标记**: 使用 Hooks、浏览器 API 或交互功能的组件必须添加 `'use client'`
5. **Tailwind CSS**: 
   - 使用 `cn()` 函数合并类名（来自 `@/lib/utils`）
   - 遵循移动优先原则
   - 使用 `dark:` 前缀支持暗色模式
6. **TypeScript**: 
   - 避免使用 `any`，使用 `unknown` 或具体类型
   - 组件 Props 使用 `interface` 或 `type` 定义
7. **localStorage 使用**: 
   - 所有 localStorage 操作必须通过封装的工具函数
   - 使用统一的存储键名常量
   - 处理存储失败的情况（try-catch）
   - 考虑浏览器隐私模式可能禁用 localStorage

## 🚀 开发顺序建议

1. **第一步**: 搭建基础结构（目录、类型定义、环境变量）
2. **第二步**: 实现 Token 和 Cookie 管理（核心功能）
3. **第三步**: 实现 API 客户端（测试 API 连接）
4. **第四步**: 实现书籍管理功能
   - localStorage 封装
   - URL 解析模块
   - 书籍管理模块
   - 阅读进度管理
   - 相关 Hooks
5. **第五步**: 开发书籍管理 UI 组件（URL 输入、书籍列表、书籍卡片）
6. **第六步**: 开发文章阅读组件（UI 展示）
7. **第七步**: 集成和测试（端到端测试）
8. **第八步**: 优化和打磨（样式、性能、体验）

## 📋 检查清单

### 功能完整性
- [ ] 开发环境 API 请求正常
- [ ] 生产环境 JWT 认证正常
- [ ] Token 正确存储和读取
- [ ] Cookie 加密/解密正常
- [ ] 文章内容正确显示
- [ ] 章节导航功能正常
- [ ] URL 解析功能正常（正确格式、错误格式处理）
- [ ] 书籍添加功能正常
- [ ] 书籍删除功能正常
- [ ] 书籍列表显示正常
- [ ] 阅读进度保存正常
- [ ] 继续阅读功能正常
- [ ] localStorage 持久化正常
- [ ] 错误处理完善

### 代码质量
- [ ] TypeScript 类型定义完整
- [ ] 代码符合 ESLint 规范
- [ ] 组件结构清晰
- [ ] 错误处理完善
- [ ] 代码注释充分

### 用户体验
- [ ] 加载状态清晰
- [ ] 错误提示友好
- [ ] 响应式布局正常
- [ ] 阅读体验良好
- [ ] 交互流畅

---

## 📝 变更记录

> **维护说明**: 每次需求变更时，在此章节添加一条记录，并更新相关章节内容。


---

## 🔧 文档维护流程

### 当需求发生变更时

#### 步骤 1: 快速记录变更（1分钟）
在"变更记录"章节添加一条新记录：

```markdown
### YYYY-MM-DD - [变更标题]
- **变更**: [简要描述变更内容]
- **原因**: [为什么需要这个变更]
- **影响模块**: [列出受影响的模块/文件]
- **状态**: ⏸️ 待开发
```

#### 步骤 2: 更新相关章节（5-10分钟）
根据变更内容，更新以下章节（按需）：
- [ ] **需求分析**: 更新核心功能列表
- [ ] **目录结构**: 添加/修改文件结构
- [ ] **开发阶段规划**: 添加新的开发任务
- [ ] **技术实现细节**: 补充实现说明
- [ ] **检查清单**: 添加测试项

#### 步骤 3: 更新文档头部（30秒）
- 更新"当前版本"（如 v1.1 → v1.2）
- 更新"最后更新"日期
- 更新"开发状态"（🟢 进行中 / 🟡 暂停 / 🔴 已完成）

#### 步骤 4: Git 提交（30秒）
```bash
git add 开发计划.md
git commit -m "docs: 更新开发计划 - [变更描述]"
```

### 状态标记说明

- ✅ **已完成**: 功能已开发完成并测试通过
- 🔄 **进行中**: 正在开发中
- ⏸️ **待开发**: 计划开发，但尚未开始
- ❌ **已取消**: 需求已取消，不再开发
- 🔴 **阻塞中**: 遇到问题，暂时无法继续

### 定期维护（建议每周一次）

1. **检查已完成项**: 将已完成的任务标记为 ✅
2. **更新进行中任务**: 更新任务状态和进度
3. **清理已取消项**: 标记为 ❌ 或删除
4. **更新日期**: 更新"最后更新"日期
5. **回顾变更**: 检查变更记录是否完整

### 快速查找技巧

- 搜索 `## 📝 变更记录` - 查找所有变更
- 搜索 `- [ ]` - 查找所有待办任务
- 搜索 `- [x]` - 查找所有已完成任务
- 搜索状态标记（✅🔄⏸️❌） - 快速定位任务状态

---

**计划制定时间**: 2025年

